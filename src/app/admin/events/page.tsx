import AdminEvents from "@/components/admin/AdminEvents";

export const metadata = {
  title: "Events Admin — Covenant Renewers",
};

export default function EventsAdminPage() {
  return <AdminEvents />;
}
