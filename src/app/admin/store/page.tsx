import AdminStore from "@/components/admin/AdminStore";

export const metadata = {
  title: "Store Admin — Covenant Renewers",
};

export default function StoreAdminPage() {
  return <AdminStore />;
}
